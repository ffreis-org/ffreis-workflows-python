name: Python Container Build

on:
  workflow_call:
    inputs:
      image-name:
        description: "Name (and optional tag) for the container image"
        type: string
        required: true
      containerfile:
        description: "Path to the Containerfile or Dockerfile"
        type: string
        default: "Containerfile"
      context:
        description: "Build context directory"
        type: string
        default: "."
      build-args:
        description: "Newline-separated build-arg pairs (KEY=VALUE)"
        type: string
        default: ""
      run_on_draft:
        description: "Run even on draft PRs (default: skip on draft to save CI minutes)"
        required: false
        type: boolean
        default: false
      runner:
        description: |
          JSON-encoded runner label(s). Default: GitHub-hosted ubuntu-latest.
        required: false
        type: string
        default: '["ubuntu-latest"]'

jobs:
  container-build:
    if: ${{ inputs.run_on_draft || vars.CI_RUN_ON_DRAFT == 'true' || github.event_name != 'pull_request' || github.event.pull_request.draft == false }}
    name: Container Build
    permissions:
      contents: read
    # NOTE: self-hosted routing via `runner:` is added for future-readiness, but
    # cannot actually run there until a container runtime is provisioned
    # (separate, deliberately-deferred decision). Default stays ubuntu-latest.
    runs-on: ${{ fromJson(inputs.runner) }}
    timeout-minutes: 30

    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 # v2.19.1
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Detect container engine
        id: engine
        run: |
          set -euo pipefail
          if command -v podman &>/dev/null; then
            echo "bin=podman" >> "$GITHUB_OUTPUT"
          elif command -v docker &>/dev/null; then
            echo "bin=docker" >> "$GITHUB_OUTPUT"
          else
            echo "Neither podman nor docker found in PATH" >&2
            exit 1
          fi

      - name: Build image
        env:
          ENGINE: ${{ steps.engine.outputs.bin }}
          CONTAINERFILE: ${{ inputs.containerfile }}
          IMAGE_NAME: ${{ inputs.image-name }}
          BUILD_ARGS_INPUT: ${{ inputs.build-args }}
          CONTEXT: ${{ inputs.context }}
        run: |
          set -euo pipefail
          BUILD_ARGS=()
          if [ -n "$BUILD_ARGS_INPUT" ]; then
            while IFS= read -r arg; do
              [ -z "$arg" ] && continue
              BUILD_ARGS+=(--build-arg "$arg")
            done <<< "$BUILD_ARGS_INPUT"
          fi
          "$ENGINE" build \
            -f "$CONTAINERFILE" \
            -t "$IMAGE_NAME" \
            "${BUILD_ARGS[@]+"${BUILD_ARGS[@]}"}" \
            "$CONTEXT"

      - name: Smoke test image
        env:
          ENGINE: ${{ steps.engine.outputs.bin }}
          IMAGE_NAME: ${{ inputs.image-name }}
        run: |
          set -euo pipefail
          "$ENGINE" run --rm "$IMAGE_NAME" --help \
            || "$ENGINE" run --rm "$IMAGE_NAME" true
